import { Header } from "../components/Header";

export const Compare = () => {
    return (
        <div className="w-full min-h-screen relative fadeIn flex flex-col items-center">
            <Header />
            <br />
            <h1 className="text-3xl font-bold text-center">Comparism Form
            </h1>
        </div>
    );
}